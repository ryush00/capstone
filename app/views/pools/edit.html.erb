<% content_for :title, "Editing pool" %>

<div class="container mt-5" style="width:40%">
  <!-- 제목 -->
  <h1 class="text-center mb-4">카풀 수정</h1>

  <!-- 폼 카드 -->
  <div class="card shadow-sm">
    <div class="card-body">
      <%= render "form", pool: @pool %>
    </div>
  </div>

  <!-- 버튼 링크 -->
  <div class="mt-3 text-end">
    <%= link_to "자세히 보기", @pool, class: "btn btn-success me-2" %>
    <%= link_to "목록으로", pools_path, class: "btn btn-secondary" %>
  </div>
</div>
